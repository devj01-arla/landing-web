---
export const prerender = false;

// src/pages/panel.astro
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Panel | Arla & Asociados">
  <Navbar />

  <main class="min-h-screen pt-24 pb-16 bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 relative overflow-hidden">
    <!-- Fondo decorativo -->
    <div class="pointer-events-none absolute inset-0 -z-10">
      <div class="absolute -right-40 -top-40 h-80 w-80 rounded-full bg-arla-blue/20 blur-3xl"></div>
      <div class="absolute -left-20 bottom-0 h-72 w-72 rounded-full bg-emerald-400/10 blur-3xl"></div>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.05),_transparent_60%)]"></div>
    </div>

    <section class="mx-auto flex w-full max-w-6xl flex-col gap-10 px-4 sm:px-6 lg:px-8">
      <!-- Encabezado + botón de logout -->
      <header class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
        <div class="space-y-3">
          <p class="inline-flex items-center gap-2 rounded-full bg-white/5 px-4 py-1 text-xs font-medium text-slate-200 shadow-sm ring-1 ring-white/10 backdrop-blur">
            <span class="h-2 w-2 rounded-full bg-emerald-400 animate-pulse"></span>
            Sesión iniciada correctamente
          </p>

          <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold tracking-tight text-white font-poppins">
            Bienvenido al panel de <span class="text-arla-blue">Arla</span>
          </h1>
          <p class="max-w-2xl text-sm sm:text-base text-slate-300 font-inter">
            Desde aquí podrás gestionar tu cuenta, revisar la información de tu empresa y acceder
            a las herramientas que estamos preparando para ti. Esta vista es solo un ejemplo,
            puedes adaptarla según las funcionalidades reales de tu proyecto.
          </p>
        </div>

        <!-- Botón de cierre de sesión -->
        <button
          id="logoutButton"
          class="inline-flex items-center justify-center rounded-xl border border-white/20 bg-white/10 px-4 py-2 text-xs sm:text-sm font-semibold text-slate-100 shadow-lg backdrop-blur transition hover:bg-red-500/90 hover:border-red-400 hover:text-white"
          type="button"
        >
          <svg
            class="mr-2 h-4 w-4"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M17 16l4-4m0 0l-4-4m4 4H9m4 4v1a2 2 0 01-2 2H7a2 
                 2 0 01-2-2V7a2 2 0 012-2h4a2 2 0 012 2v1" />
          </svg>
          Cerrar sesión
        </button>
      </header>

      <!-- Tarjetas principales -->
      <section class="grid gap-6 md:grid-cols-3">
        <!-- Tarjeta 1 -->
        <div class="group rounded-2xl border border-white/10 bg-white/5 p-5 shadow-xl backdrop-blur-lg transition-all duration-300 hover:-translate-y-1 hover:border-arla-blue/60 hover:bg-white/10">
          <div class="mb-3 flex items-center justify-between">
            <h2 class="text-sm font-semibold text-white font-poppins">
              Resumen de tu cuenta
            </h2>
            <span class="rounded-full bg-emerald-500/10 px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-emerald-300">
              Activo
            </span>
          </div>
          <p class="text-xs sm:text-sm text-slate-300 font-inter mb-4">
            Aquí podrías mostrar datos del usuario: nombre de empresa, correo, fecha de registro,
            y cualquier otra información relevante que obtengas desde tu API.
          </p>
          <div class="space-y-2 text-xs text-slate-300 font-inter">
            <div class="flex items-center justify-between rounded-lg bg-black/20 px-3 py-2">
              <span>Estado</span>
              <span class="font-medium text-emerald-300">Verificado</span>
            </div>
            <div class="flex items-center justify-between rounded-lg bg-black/20 px-3 py-2">
              <span>Último acceso</span>
              <span class="font-medium text-slate-100">Hace unos instantes</span>
            </div>
          </div>
        </div>

        <!-- Tarjeta 2 -->
        <div class="group rounded-2xl border border-white/10 bg-white/5 p-5 shadow-xl backdrop-blur-lg transition-all duration-300 hover:-translate-y-1 hover:border-arla-blue/60 hover:bg-white/10">
          <div class="mb-3 flex items-center justify-between">
            <h2 class="text-sm font-semibold text-white font-poppins">
              Próximas acciones
            </h2>
            <span class="rounded-full bg-sky-500/10 px-2.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide text-sky-300">
              Sugerencias
            </span>
          </div>
          <ul class="space-y-2 text-xs sm:text-sm text-slate-300 font-inter">
            <li class="flex items-start gap-2">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-arla-blue"></span>
              <span>Completar el perfil de tu empresa.</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-arla-blue"></span>
              <span>Configurar los datos de contacto y facturación.</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1 h-1.5 w-1.5 rounded-full bg-arla-blue"></span>
              <span>Revisar las secciones disponibles en el menú principal.</span>
            </li>
          </ul>
        </div>

        <!-- Tarjeta 3 -->
        <div class="group flex flex-col justify-between rounded-2xl border border-white/10 bg-gradient-to-br from-arla-blue/80 to-sky-500/80 p-5 shadow-xl backdrop-blur-lg transition-all duration-300 hover:-translate-y-1">
          <div>
            <h2 class="mb-2 text-sm font-semibold text-white font-poppins">
              ¿Necesitas ayuda?
            </h2>
            <p class="mb-4 text-xs sm:text-sm text-slate-100 font-inter">
              Si tienes alguna duda sobre el funcionamiento del panel o deseas habilitar nuevas
              funcionalidades, puedes ponerte en contacto con nuestro equipo.
            </p>
          </div>
          <a
            href="/#contact"
            class="inline-flex items-center justify-center rounded-xl bg-white/95 px-4 py-2 text-xs sm:text-sm font-semibold text-arla-blue shadow-lg transition hover:bg-white"
          >
            Contactar soporte
            <svg
              class="ml-2 h-4 w-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </section>

      <!-- Sección inferior (placeholder de contenido futuro) -->
      <section class="mt-4 rounded-2xl border border-dashed border-white/15 bg-black/20 p-6 text-sm sm:text-base text-slate-300 font-inter">
        <h2 class="mb-2 text-base sm:text-lg font-semibold text-white font-poppins">
          Zona de contenido futuro
        </h2>
        <p class="mb-3">
          Aquí podrías añadir tablas, gráficos, un listado de clientes, historial de acciones,
          un chatbot, o cualquier módulo que forme parte del producto final.
        </p>
        <p class="text-xs sm:text-sm text-slate-400">
          Por ahora es solo un contenedor de ejemplo para que tengas un punto de partida visual.
        </p>
      </section>
    </section>
  </main>

  <Footer />

  <!-- Script de logout -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const btn = document.getElementById('logoutButton');
      if (!btn) return;

      btn.addEventListener('click', async () => {
        // feedback visual
        btn.setAttribute('disabled', 'true');
        const originalText = btn.textContent;
        btn.textContent = 'Cerrando sesión...';

        try {
          const res = await fetch('/api/logout', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
          });

          if (res.ok) {
            window.location.href = '/login';
          } else {
            alert('No se pudo cerrar sesión. Intente nuevamente.');
            btn.removeAttribute('disabled');
            btn.textContent = originalText;
          }
        } catch (err) {
          console.error(err);
          alert('Error al conectar con el servidor.');
          btn.removeAttribute('disabled');
          btn.textContent = originalText;
        }
      });
    });
  </script>
</Layout>
